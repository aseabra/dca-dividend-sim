Simulate investing a fixe amount each month into one stock/ETF and compare :
- dividends paid out (tracked as cash)
- dividends reinvested (DRIP)

##SETUP
```bash
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt```